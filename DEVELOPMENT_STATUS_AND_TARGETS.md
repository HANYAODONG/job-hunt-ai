# JobMatch AI 揭榜挂帅开发现状与预期目标

> 项目：JobMatch AI
>
> 赛题：XH-202621「多源异构数据驱动岗位和能力图谱构建与动态演化分析研究」
>
> 版本基线：2026-07-16，仓库 `main` 分支
>
> 用途：作为产品、前端、后端、算法、数据和答辩工作的统一进度基线。每次迭代完成后更新“进度记录”和对应任务状态。

## 1. 项目定位

当前仓库是一个英文求职匹配 Demo，核心能力是自然语言搜岗、简历解析、岗位推荐、混合检索和知识图谱匹配。

后续参赛产品需要升级为面向新一代信息技术岗位的“岗位能力图谱动态演化与人岗诊断系统”，形成以下闭环：

```text
多源岗位数据
    -> 数据清洗、去重、时效与可信度评估
    -> 新岗位发现 / 既有岗位能力更新
    -> 岗位-能力全景图谱
    -> 简历解析与候选人能力画像
    -> 人岗匹配、差距分析
    -> 改进建议与岗位学习路径
    -> 人工审核、反馈回流、图谱持续演化
```

产品同时服务两类业务视角：

- **求职者**：上传简历，理解自身能力，获得岗位匹配、差距分析和学习路径。
- **企业招聘方**：维护本企业的招聘 JD，处理候选人投递，依据可解释的匹配结果完成筛选与人工确认。

两端共享岗位能力图谱、岗位版本、简历解析和匹配能力，但不共享页面任务。数据治理和评测结果用于证明系统可信度，应在落地页集中展示，不应占用日常招聘或求职导航。

## 2. 当前开发现状

### 2.1 仓库与运行架构

- 前端：`frontend-src`，React 18、React Router、Ant Design、Axios、React Query、Recharts。
- 后端：`backend-src`，FastAPI、spaCy、Sentence Transformers、Elasticsearch、Neo4j。
- 部署：`docker-compose.yml` 编排 Elasticsearch、Neo4j、FastAPI 后端和 React 前端。
- 样本数据：`jobs/SDE-Nov21.csv`，约 11,305 条英文软件开发岗位记录。
- 主要入口：前端 `http://localhost:18080`，后端 `http://localhost:8000`，API 文档 `http://localhost:8000/docs`。
- 当前仓库未发现完整的前端/后端自动化测试目录和覆盖率基线，需补建测试体系。

### 2.2 已有前端页面

- 首页：产品介绍、搜索入口、简历上传入口、统计信息。
- 搜索页：自然语言搜索、地点、薪资、工作类型、经验、远程、签证等条件。
- 职位详情页：查看岗位信息、相似职位和申请入口。
- 简历上传页：上传 PDF/DOCX，触发简历处理。
- 推荐页：根据简历生成岗位推荐和匹配解释。
- 个性化推荐页：支持简历和用户描述，调用重排序推荐接口。
- 登录/注册页：已有基础认证界面。

### 2.3 已有后端能力

- Elasticsearch 关键词 / BM25 检索。
- Sentence Transformers 语义向量匹配。
- Neo4j 岗位、技能、候选人和关系查询。
- 混合召回与重排序：关键词、语义、知识图谱候选池融合。
- 简历 PDF、DOCX、TXT 文本提取和候选人画像生成。
- 技能、经历、教育、联系方式等字段抽取。
- 技能匹配、语义相似度、地点、薪资、签证等维度评分。
- CSV 和外部职位源导入接口。
- 用户注册、登录、职位申请接口。
- Anthropic API 可选，用于生成匹配解释和重排序说明。

### 2.4 当前产品定位上的限制

- 现有界面和样本数据以英文海外求职为主，不符合国内学生竞赛场景。
- 当前重点是“找工作”，不是“岗位能力研究和演化分析”。
- Neo4j 有图谱后端能力，但缺少面向评审的图谱可视化前端。
- 没有新岗位发现、岗位能力版本比较和动态演化时间轴。
- 没有明确的数据来源、证据、置信度和人工审核闭环。
- 简历解析和匹配已有算法基础，但没有达到赛题要求的可验证指标体系。

## 3. 预期实现目标

### 3.1 产品目标

交付一个可部署、可演示、可量化验证的中文岗位能力智能分析平台，至少支持：

1. 面向人工智能、大数据、智能系统、物联网等方向采集和管理岗位数据。
2. 发现市场中新兴岗位，并生成可人工编辑的岗位定义。
3. 对既有岗位进行版本化能力更新，清楚展示技能新增、删除和修改。
4. 以技能点粒度展示岗位能力全景图谱，支持领域、技术栈和职级切换。
5. 解析 PDF/Word 简历，生成候选人技能画像。
6. 输出人岗匹配度、已具备能力、缺失能力、差距原因和学习路径。
7. 展示数据来源、更新时间、交叉验证结果、置信度和人工审核记录。

### 3.2 已确认的业务对象与边界

| 对象 | 定义 | 使用方 |
|---|---|---|
| 标准岗位画像 | 市场与图谱中的规范岗位定义，包含职责、技能、技术栈、职级和版本。 | 全系统共享 |
| 岗位版本 | 标准岗位画像在某一时间点的已发布版本，明确能力项新增、删除和修改。 | 全系统共享 |
| 企业招聘 JD | 企业实际发布或维护的招聘需求，关联一个标准岗位版本，但可增加本企业的地点、人数、业务场景和个性化要求。 | 企业招聘方 |
| 市场信号 | 从外部 JD 和行业资料中识别出的新岗位、技能增减、岗位演变或消失候选，必须附带来源、时间和置信度。 | 平台、企业招聘方 |
| 候选人画像 | 从简历解析出的技能、经历、教育和能力证据。 | 求职者、企业招聘方 |
| 匹配报告 | 某候选人相对于某个标准岗位或企业招聘 JD 的匹配度、已具备能力、缺失能力和解释证据。 | 求职者、企业招聘方 |
| 学习路径 | 基于求职者选定目标岗位和匹配报告生成的改进任务、作品交付物和进度。 | 求职者 |

约束：市场信号只能提出标准岗位或企业招聘 JD 的更新建议，不能自动修改企业已发布 JD；企业必须人工确认后才形成新的 JD 修订版本。

### 3.3 目标信息架构

```text
落地页
├── 进入求职者端或企业端
└── 系统可信度展示：数据来源、治理结果、100+ JD 测试集、三类准确率、误差示例

求职者端
├── 全景图谱：搜索、筛选、定位岗位节点，查看技能点级岗位画像并发起诊断
├── 人岗诊断：上传简历、确认能力画像、自动匹配岗位、选择目标岗位、查看差距与依据
└── 学习路径：展示目标岗位、匹配度、关键缺口、学习任务、作品交付物和进度

企业端
├── 我的招聘：企业招聘 JD 池，支持新建、编辑、发布、暂停、版本修订和市场更新建议
├── 候选匹配：按招聘 JD 查看投递候选人、简历解析结果、匹配依据和人工筛选状态
├── 岗位市场雷达：审核外部市场中的新岗位、技能变化、岗位演变/消失和对企业 JD 的影响
└── 全景图谱：共享的岗位、技能、技术栈、职级和版本关系图谱，用于校准 JD 与理解能力结构
```

页面边界：

- 求职者端不设置独立“工作台”“岗位探索”或“匹配结果”页面；岗位探索并入全景图谱，匹配结果并入人岗诊断。
- 企业端不设置独立“学习路径”页面；企业仅在候选匹配中查看与招聘 JD 有关的能力证据和匹配结论。
- “岗位演化”作为标准岗位或企业招聘 JD 的详情视图，不再单独占用主导航。
- 数据治理和评测中心不作为主业务页面，而是在落地页的“系统可信度”区域展示；岗位市场雷达和图谱详情仍须内嵌来源、置信度和证据链。

### 3.4 前端重点目标

- 全局语言切换为中文，统一岗位、技能、职级和指标命名。
- 落地页提供清晰的双端入口，并集中展示系统可信度与量化验证结果，不使用其承载日常业务流程。
- 图谱页面支持缩放、拖拽、节点筛选、关系高亮、技术栈和职级切换；节点详情必须展示岗位职责、技能和版本信息。
- 求职者端以“上传简历 -> 自动匹配 -> 差距诊断 -> 学习路径”为唯一主流程，避免重复的摘要和浏览页面。
- 企业端以“维护招聘 JD -> 接收投递 -> 候选匹配 -> 人工确认”为唯一主流程；市场雷达只提供基于外部证据的更新建议。
- 人岗诊断和候选匹配复用同一匹配能力，但分别面向求职者的自我诊断和企业的候选人筛选。
- 市场雷达支持“AI 生成 -> 人工审核 -> 更新标准岗位画像/提出 JD 修订建议”的明确流程。
- 所有 AI 结论必须显示来源、置信度或待审核状态，避免只展示不可解释的分数。
- 设计空状态、加载、错误、低置信度、待审核和无数据等完整状态。

## 4. 赛题要求与验收指标

| 指标 | 目标值 | 验收方式 |
|---|---:|---|
| JD 解析准确率 | >= 90% | 至少 100 条标注 JD，对比技能/职责/职级等字段 |
| 简历技能提取准确率 | >= 90% | 标注简历集，计算 Precision / Recall / F1 |
| 人岗匹配准确率 | >= 90% | 专家或标注集对匹配结果进行评估 |
| 测试 JD 数量 | >= 100 条 | 固定数据集、输入输出样例、错误分析 |
| 单元测试覆盖率 | >= 60% | 后端 pytest、前端测试报告 |
| 新岗位功能 | 1 个可完整演示 | 发现、定义、审核、发布、进入图谱 |
| 既有岗位演化 | 1 个可完整演示 | 至少 2 个版本的能力增删改与来源 |
| 部署可用性 | 一键部署 | Docker Compose 在干净环境启动并完成演示 |

## 5. 分阶段开发路线

### 阶段 0：基线整理

- [ ] 固定仓库版本、运行说明、环境变量和服务端口。
- [ ] 确认中文岗位数据来源和数据授权范围。
- [ ] 确认参赛演示的 1 个新岗位、1 个既有岗位。
- [ ] 建立需求、接口、数据字典和指标文档。

### 阶段 1：数据基础

- [ ] 建立岗位、技能、职责、职级、技术栈、来源、版本等统一数据模型。
- [ ] 接入至少两类岗位数据源。
- [ ] 完成字段标准化、去重、更新时间和来源记录。
- [ ] 增加 JD 质量评分、重复检测和异常数据标记。

### 阶段 2：岗位能力图谱

- [ ] 建立岗位-技能-职责-技术栈-职级关系。
- [ ] 完成技能点粒度的岗位图谱查询接口。
- [ ] 实现前端全景图谱、筛选、详情和关系高亮。
- [ ] 建立岗位能力版本和变更记录。

### 阶段 3：新岗位发现与演化

- [ ] 基于趋势、频次、共现和聚类发现候选新岗位。
- [ ] 生成岗位名称、职责、必备技能、加分技能和应用场景。
- [ ] 增加证据引用、置信度、人工审核和发布状态。
- [ ] 支持既有标准岗位能力新增、删除、修改的对比和回滚。
- [ ] 将市场变化转化为企业招聘 JD 的人工修订建议，保留企业确认记录。

### 阶段 4：双端人岗匹配

- [ ] 优化中文 PDF/Word 简历解析。
- [ ] 输出候选人技能、经验、职级和证据。
- [ ] 求职者端：展示自动岗位匹配、目标岗位选择、匹配度、强项、缺口、缺口优先级和原因。
- [ ] 企业端：按企业招聘 JD 处理投递候选人，展示匹配依据并支持待筛选、待沟通、入围和不匹配等人工状态。
- [ ] 为求职者生成岗位学习路径，并标注推荐依据；企业端不展示默认学习路径。

### 阶段 5：评测与比赛交付

- [ ] 建立 100+ 条 JD 和测试用例基准集。
- [ ] 完成 JD、简历和匹配三类准确率评测。
- [ ] 补齐单元测试、集成测试和覆盖率报告。
- [ ] 完成演示数据、PPT、部署说明、源代码和十分钟演示视频。
- [ ] 完成一次干净环境 Docker 部署演练。

## 6. 进度判定规则

- **未开始**：尚无设计、接口或代码。
- **设计中**：已完成页面/接口/数据模型设计，但未形成可运行功能。
- **开发中**：功能可运行，但缺少完整状态、测试或数据闭环。
- **联调中**：前后端和真实数据已打通，正在修复流程问题。
- **可验收**：满足功能、数据、指标和部署要求，并有截图/视频/测试证据。
- **已交付**：已进入比赛材料和最终演示版本，不再随意变更核心流程。

## 7. 每次迭代记录模板

```markdown
### YYYY-MM-DD 迭代记录

- 本次目标：
- 完成内容：
- 涉及模块：
- 当前状态：未开始 / 设计中 / 开发中 / 联调中 / 可验收 / 已交付
- 新增接口或数据：
- 指标变化：
- 已知问题：
- 下一步：
- 验证证据：截图、测试报告、演示视频或提交记录
```

## 8. 当前最优先事项

1. 固化本文件中的双端信息架构和业务对象，后续页面与接口不得新增重复职责。
2. 先完成“全景图谱 + 求职者人岗诊断/学习路径 + 企业招聘 JD/候选匹配”四条核心流程的低保真原型。
3. 再补市场雷达、标准岗位版本、数据模型和后端接口，确保市场变化可以追溯到图谱和企业 JD 修订建议。
4. 使用真实或固定的测试数据完成 100+ JD 和三类准确率验证，并将数据治理和评测结果放入落地页的系统可信度展示。
5. 最后完善视觉细节、测试证据、部署说明和比赛材料。
